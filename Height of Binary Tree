class Solution {
  public:
    int height(Node* root) {
        if(root == NULL)
            return -1; //edge based calculation
        
        int leftHeight = height(root->left);
        int rightHeight = height(root->right);
        
        int ans = 1 + max(leftHeight, rightHeight);
        return ans;
        
    }
};
